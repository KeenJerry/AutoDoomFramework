<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="SelectLocationButtonTemplate" TargetType="Button">
        <Border Background="Transparent" BorderBrush="{StaticResource LightBorder}" BorderThickness="0 1 1 1" x:Name="Background">
            <Image Source="/Assets/Images/bx-folder.png"/>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Background" Property="Background" Value="{StaticResource LightBorder}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>